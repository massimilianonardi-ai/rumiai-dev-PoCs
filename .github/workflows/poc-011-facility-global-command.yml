name: PoC 011 facility default global commands

on:
  push:
    branches:
      - main
    paths:
      - 'pocs/011-facility-default-global-command-projection/**'
      - '.github/workflows/poc-011-facility-global-command.yml'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run PoC
        shell: sh
        run: pocs/011-facility-default-global-command-projection/tests/run
