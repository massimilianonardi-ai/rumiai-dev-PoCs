name: PoC 009 provider selector resolution

on:
  push:
    branches:
      - main
    paths:
      - 'pocs/009-package-provider-selector-resolution/**'
      - '.github/workflows/poc-009-provider-selector.yml'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run PoC
        shell: sh
        run: pocs/009-package-provider-selector-resolution/tests/run
